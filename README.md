# Hakisoccer

A same-phone two-player physics soccer shooter built as a single vanilla HTML file.

- Open `index.html` directly to play offline.
- The deployed Worker serves `public/index.html`.
- Cloudflare deploy command: `wrangler deploy --domain soccer.hakanalpay.com`
